<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lzk.jetpacktest">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MainApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/networkconfig"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".coroutine.CoroutineActivity"></activity>
        <activity android:name=".workmanager.WorkManagerActivity" />
        <activity android:name=".paging.GirlActivity" />
        <activity android:name=".navigation.NavigationActivity" />
        <activity android:name=".room.RoomActivity" />
        <activity android:name=".LiveData.LiveDataActivity" />
        <activity android:name=".lifecycle.CusLifeCycleActivity" />
        <activity android:name=".lifecycle.LifeCycleActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>